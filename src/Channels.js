import React, {useState} from 'react';

const styles = {

    channels: {
        minWidth: 200
    }

};

export default ({

}) => {
    return (
        <div style={styles.channels}>
                </div>);
}